import React from 'react';

const PageTitle = ({ title }) => {
  return (
    <h1 className="text-4xl text-slate-100 font-bold mb-8 text-center font-montserrat">{title}</h1>
  );
};

export default PageTitle;
